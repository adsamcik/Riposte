<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme for dark mode -->
    <style name="Theme.Riposte.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Riposte</item>
    </style>

    <!-- Main app theme for dark mode -->
    <style name="Theme.Riposte" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <!-- Splash screen colors for dark mode -->
    <color name="splash_background">#1C1B1F</color>
    <color name="splash_icon_background">#BB86FC</color>
</resources>
